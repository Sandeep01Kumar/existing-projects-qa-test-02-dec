// Pass explicitly to listen to keep the server bound to loopback.
const HOSTNAME = '127.0.0.1';

const PORT = 3000;

const MEDIA_TYPE = 'text/plain';

module.exports = { HOSTNAME, PORT, MEDIA_TYPE };
